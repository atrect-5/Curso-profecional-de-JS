
var variable = 0

do{ // Se ejecuta una vez antes de evaluar
    variable++
}while(variable <= 10)

alert(`La variable vale ${variable}`) // variable = 11, porque se eecuta 1 vez mas antes de evaluar